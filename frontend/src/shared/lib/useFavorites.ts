// Re-exportado desde el Context compartido (`app/providers/FavoritesProvider`).
// Se mantiene este archivo/path para no tocar los imports existentes
// (ProductCard, ProductDetailPage, FavoritesPage). Ver ese archivo para el
// motivo del cambio de hook-plano a Context.
export { useFavorites } from "@/app/providers/FavoritesProvider";
