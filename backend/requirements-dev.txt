pytest>=9.1.1
pytest-asyncio>=0.23
httpx>=0.27,<0.28
ruff>=0.8,<1.0
# Solo para los tests: genera PDF con tablas reales para probar la extracción
# de facturas. No se usa en runtime -- el backend no genera PDF, solo los lee.
# Un PDF de fixture commiteado sería más liviano, pero un binario opaco en el
# repo no deja ver qué se está probando.
reportlab>=4.0,<5.0
