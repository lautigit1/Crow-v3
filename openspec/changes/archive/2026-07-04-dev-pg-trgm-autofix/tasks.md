# Tasks: dev-pg-trgm-autofix

## Implementation tasks

- [x] **T1** — Importar `text` de sqlalchemy en `backend/app/main.py`
- [x] **T2** — Agregar `CREATE EXTENSION IF NOT EXISTS pg_trgm` (try/except, solo Postgres) en la rama dev del lifespan
- [x] **T3** — Verificar sintaxis/consistencia
