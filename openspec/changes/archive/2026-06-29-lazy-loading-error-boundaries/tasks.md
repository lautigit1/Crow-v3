# Tasks: lazy-loading-error-boundaries

- [x] **T1** — Crear `ErrorBoundary.tsx` en `frontend/src/app/providers/`
- [x] **T2** — Envolver `<App>` en `main.tsx` con `<ErrorBoundary>`
- [x] **T3** — Convertir imports de páginas admin y account a `React.lazy` en `App.tsx`
- [x] **T4** — Agregar `<Suspense>` y `<ErrorBoundary>` para la sección admin en `App.tsx`
