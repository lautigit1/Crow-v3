{
  "name": "crow-repuestos",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Herramientas de nivel repo (git hooks, lint de commits). El código vive en frontend/ y backend/, cada uno con su propio gestor de dependencias.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.6"
  }
}
